'use strict';

// if(조건)
// ! 저 조건이 boolean으로 true일 때 실행된다!
// 즉 우리 boolean true가 나오는 값들이나,
// or 비교연산자, 동등비교연산자는 boolean을 return하니까
// 값이나 비교하는 연산자를 써줘서 true를 반환할 수 있음.
// ! or boolean type을 return하는 함수도 들어올 수 있음. 

// 삼항연산자도 쓸 수 있음!! 
// 간단한 if문은 삼항연산자 쓰는게 좋아!!
if(!0){
    console.log(`true값!!`)
}else{
    console.log('false');
}