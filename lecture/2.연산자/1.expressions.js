'use strict';
// Expressions(표현식)
// Statement중 값으로 평가되는 식!
// ex) 할당, 숫자, 문자열, 연산식 등등.

// statement(모든 문장들은 다 statement다!!)
let myName; // 선언`문` : js engine에게 변수를 등록해줌.
myName = "김지성" // 할당`문` && 표현식

// 즉 값은 표현식이며 변수에 할당할 수 있다.
// ! 절대 단어를 연상하면서 코딩을 하진 않아!! 
// 어떤 값이 들어있고, 이 값이 어디로 할당되고~ 이걸 알아야돼!
// ! 코드의 흐름을 파악하면서 코딩하자!