'use strict';
// 프로그램 3단계

// 1. input(입력을 받음)
// 2. process(이 부분을 배우는거야!!!!!)
// 3. output(출력)

// 비트연산자는 다루지 않음. 
// 프론트, 백에서는 거의 사용되지 않음.


// 표 현 식
// 값 => primitive, object로 나뉨.

// 리터럴(Literal)
// 값을 나타내는 표기법(그 값을 어떻방식으로 써?)
// "", ''는 문자열 리터럴
// ``템플릿 리터럴
// 3 0x, 0b, 0o 등등.. 숫자 리터럴
// true, false 불린 리터럴
// {} 객체 리터럴
// [] 배열 리터럴

// Statement(문)
// 프로그램이 최소한으로 실행될 수 있는 단위(한 문장)

// if`문` 반복`문` 실행`문`

// 표현식(Expressions)
//! Statement중 `값`으로 평가가 된다면 이를 표현식이라고 해!
