'use strict';

// 사물함에 들어가있는 값의 형식! 
// 지금까지 변수 공간을 배웠고, 이제는 그 공간안에 어떤 type을 넣을 수 있는지 배움.


// type의 종류
// 1) primitive(원시타입) => 단일 데이터
// - number(모든 숫자, 다른진수도 포함)
// - string(문자열)
// - boolean(이거 아니면 저거)
// - null(아무 값이 없음을 말해줌.)
// - undefined(아직 값이 정해지지않음 => 할당이 아직 안 됨.)
// - Symbol

// 2) object(객체타입) => 복합 데이터
// - Object
// - Function
// - Array